{
  "name": "dreambuild",
  "version": "1.0.0",
  "description": "DreamBuild — Recruitment lead gen with Enneagram + AI resume matching",
  "main": "public/index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^19.0.0"
  }
}
